{
  "name": "systemusers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "npm install && typings install && rm -fr dist && tsc --outDir dist",
    "start": "node dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "GPLv3",
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.1"
  },
  "dependencies": {
    "config": "^1.21.0",
    "node-fetch": "^1.5.3"
  }
}
